// const myTable =[ "salut", 7, "Hello", "bonjour", 
// 1, 2, true, 7, "Next"];

// // This is the syntax of a method, you must not change it !

// // map(e => e > 5) (e is an arbitrary value, it can be the name of your dog, we use e as an standard to refer to element)
// // like i to refer index
// // map iterate over every element in the table that you chain to it and apply a condition or an instruction

// // chaining methods, filter().map().sort()
// const hello = myTable.filter(e => typeof e === 'string').map(e => e.toLowerCase()).sort()

// console.log(hello);




//Destructive & Non-destructive Methods

const database = [1,2,3,4,5]

const filtered = database.shift()

console.log


